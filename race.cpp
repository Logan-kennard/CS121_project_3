#include <iostream>
#include <cstdlib>
#include <time>

void advance(int hoursNum, int* horses){;
	srand(time(NULL));
	coin=rand() % 2;
	if(coin==1){hoursNum ++}; 
}
void printLane(int hoursNum, int* hourses){;
	for i in hoursNum:
	if( i >= 14) {advance} 
	else{print "."} 
}
bool isWinner(int hoursNum, int* hourses){;
	winner=true 
	keepGoing=false
       while true 
	if(printLane==14){winner}
	else{keepGoing}
}
		

int main(){
	hourses[]=(0,1,2,3,4)
	for 
